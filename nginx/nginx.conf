# Phase 0 stub: plain HTTP boot check only.
# Phase 1 replaces this with HTTPS termination, /api proxy to backend,
# and static SPA serving.

events {}

http {
    server {
        listen 80;

        location / {
            return 200 "nginx placeholder OK\n";
            add_header Content-Type text/plain;
        }
    }
}
